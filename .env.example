# API Configuration (inyectada por Terraform ECS)
VITE_API_BASE_URL=${VITE_API_BASE_URL}

# RSA Public Key (inyectada por Terraform ECS)
VITE_RSA_PUBLIC_KEY=${VITE_RSA_PUBLIC_KEY}

# AES Secret Key (inyectada por Terraform ECS)
VITE_AES_SECRET_KEY=${VITE_AES_SECRET_KEY}

# Configuración de la aplicación (inyectada por Terraform ECS)
VITE_APP_NAME=${VITE_APP_NAME}
VITE_APP_VERSION=${VITE_APP_VERSION}
VITE_APP_ENV=${VITE_APP_ENV}

# Features (inyectadas por Terraform ECS)
VITE_ENABLE_HEALTH_CHECK=${VITE_ENABLE_HEALTH_CHECK}
VITE_DEBUG_MODE=${VITE_DEBUG_MODE}
